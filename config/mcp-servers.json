{
  "$schema": "https://supr.local/schemas/mcp-server-registry.json",
  "version": 1,
  "description": "MCP server registry. Subagents must use the unified /api/mcp/* routes to talk to these servers — direct connections bypass governance.",
  "servers": [
    {
      "id": "supr-internal",
      "name": "Supr Internal Tools",
      "transport": "in-process",
      "description": "Built-in Supr tool registry (shell, web_search, project_flow, web_scrape, etc.). Always available.",
      "required_tier": "Observe",
      "tools": "auto:lib/tools/register",
      "resources": "auto:lib/db:tables",
      "enabled": true
    },
    {
      "id": "supr-skills",
      "name": "Supr Skill Catalog",
      "transport": "in-process",
      "description": "Structured skill catalog under .agents/skills/. Each skill exposes a trigger list and a system-prompt override.",
      "required_tier": "Observe",
      "resources": "auto:.agents/skills:*.md",
      "enabled": true
    },
    {
      "id": "supr-memory",
      "name": "Supr Memory Bank",
      "transport": "in-process",
      "description": "Long-term memory sections and per-mission memory items. Read/write gated by Draft tier.",
      "required_tier": "Draft",
      "resources": "auto:lib/memory:items",
      "enabled": true
    },
    {
      "id": "supr-composio",
      "name": "Supr Composio Bridge",
      "transport": "in-process",
      "description": "Bridges to third-party SaaS apps (GitHub, Slack, Notion, Gmail, etc.) via the Composio API. The composioBridge resolves the API key from Settings on every call.",
      "required_tier": "External_Act",
      "tools": "auto:composio",
      "enabled": false
    },
    {
      "id": "github-mcp",
      "name": "GitHub",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "required_tier": "Edit",
      "env_keys": ["GITHUB_PERSONAL_ACCESS_TOKEN"],
      "description": "GitHub MCP server for Automated Code Review and DevOps operations.",
      "enabled": true
    },
    {
      "id": "postgres-mcp",
      "name": "PostgreSQL",
      "transport": "stdio",
      "required_tier": "Edit",
      "env_keys": ["POSTGRES_CONNECTION_STRING"],
      "description": "Beta PostgreSQL MCP entry. Disabled until a pinned executable passes adapter certification.",
      "enabled": false
    },
    {
      "id": "filesystem-mcp",
      "name": "Filesystem",
      "transport": "stdio",
      "required_tier": "Edit",
      "env_keys": [],
      "description": "Beta filesystem MCP entry. Disabled until a pinned executable passes adapter certification; native workspace tools remain available.",
      "enabled": false
    },
    {
      "id": "brave-search-mcp",
      "name": "Brave Search",
      "transport": "stdio",
      "required_tier": "Draft",
      "env_keys": ["BRAVE_API_KEY"],
      "description": "Beta Brave Search MCP entry. Disabled until a pinned executable passes adapter certification.",
      "enabled": false
    },
    {
      "id": "context7-mcp",
      "name": "Context7 (HTTP example)",
      "transport": "http",
      "endpoint": "${env:CONTEXT7_MCP_URL|default:https://mcp.context7.com/mcp}",
      "required_tier": "Observe",
      "env_keys": ["CONTEXT7_API_KEY"],
      "description": "Reference HTTP transport example. Context7 serves up-to-date library docs. Operators can override the URL via CONTEXT7_MCP_URL and supply CONTEXT7_API_KEY for auth.",
      "enabled": false
    }
  ]
}
