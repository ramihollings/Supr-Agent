apiVersion: v1
kind: Service
metadata:
  name: supr-service
  namespace: supr
  labels:
    app: supr-orchestrator
spec:
  type: LoadBalancer
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: supr-orchestrator
